#include "main.h"
/**
 * main - tests if the function positive_or_negative()
 * gives the correct output with an input of 0
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
